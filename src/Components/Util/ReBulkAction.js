import React from 'react'

import ComingSoon from '../Util/ComingSoon'
// import ReButton from '../Button/ReButton'

const ReBulkAction = ({ list, selectedUuids }) => {
  return (
    <div className="flex-col--center">
      {/* Bulk Actions
      <ReButton type="primary">Copy To Clipboard</ReButton>
      <ReButton type="secondary">Send Email</ReButton> */}
      <ComingSoon />
    </div>
  )
}

export default ReBulkAction

import React, { Component } from 'react'
import { connect } from 'react-redux'

class ReShareTags extends Component {
  render() {
    return <div />
  }
}

const mapStateToProps = state => {
  return {}
}

export default connect(mapStateToProps)(ReShareTags)

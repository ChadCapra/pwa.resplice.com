import React from 'react'

import Card from './shared/card/Card'

const Sandbox = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
      }}
    ></div>
  )
}

export default Sandbox
